
# -------------------------------
# vprofile (Maven)
# -------------------------------
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-vprofile
  namespace: {{ .Release.Namespace }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: vprofile-dep-pod
  template:
    metadata:
      labels:
        app: vprofile-dep-pod
    spec:
      containers:
        - name: vprofile
          image: {{ .Values.images.vp.name }}:{{ .Values.images.vp.tag }} # loulah/vprofile:latest 
          ports:
            - containerPort: 8080
          envFrom:
            - configMapRef:
                name: app-config
          resources:
              requests:
                memory: "1Gi"
                cpu: "1000m"
              limits:
                memory: "2Gi"
                cpu: "2000m"

---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-vprofile-svc
  namespace: {{ .Release.Namespace }}
spec:
  type: NodePort
  selector:
    app: vprofile-dep-pod
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 32004
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  namespace: {{.Release.Namespace}}
data:
  JDBC_URL: jdbc:mysql://{{.Release.Name }}-mysql-svc.{{.Release.Namespace}}.svc.cluster.local:3306/accounts?useUnicode=true&characterEncoding=UTF-8&zeroDateTimeBehavior=convertToNull
  MEMCACHED_ACTIVE_HOST: {{ .Release.Name }}-memcache-svc.{{.Release.Namespace}}.svc.cluster.local
  MEMCACHED_STANDBY_HOST: {{ .Release.Name }}-memcache-svc.{{.Release.Namespace}}.svc.cluster.local
  RABBITMQ_ADDRESS: {{ .Release.Name }}-rabbitmq-svc.{{.Release.Namespace}}.svc.cluster.local